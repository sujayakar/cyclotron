1) cd server; cargo run -- --http=3000 --ws=3001 --traces=../examples/ --frontend=../frontend/
2) open up localhost:3000/frontend/src/index.html in browser


= TODO =
[ ] Create sprite group per lane, move rectangles onto lane
[ ] Drawing lanes in viewport is 1) deciding which lanes overlap, 2) scaling them to fit, 3) hiding the others
[ ] Then, draw text in screen space w/masks on top
[ ] Implement collapsing by logging visibility on rectangles, hiding entire lanes if invisible
[ ] Add arrows?
